[smart_terrain]
respawn_radius = 900
squad_id = 3
max_population = 2
respawn_params = respawn@ds_deb1
spawn_point = ds_deb1_spawn_point
respawn_idle = 43200

[respawn@ds_deb1]

spawn_monster1

[spawn_monster1]
spawn_squads = simulation_flesh, simulation_boar, simulation_pseudodog
spawn_num = {=!smart_controlled(ds_deb1)} 2, 0
